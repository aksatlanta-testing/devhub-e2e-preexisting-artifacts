apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo74460da5-f9de-4f32-8716-372e90337412.fullname" . }}
  labels:
    {{- include "go-echo74460da5-f9de-4f32-8716-372e90337412.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo74460da5-f9de-4f32-8716-372e90337412.selectorLabels" . | nindent 4 }}
