apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo99b4e990-95f0-4a62-ac08-03175c4d68e9.fullname" . }}
  labels:
    {{- include "go-echo99b4e990-95f0-4a62-ac08-03175c4d68e9.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo99b4e990-95f0-4a62-ac08-03175c4d68e9.selectorLabels" . | nindent 4 }}
