apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echof373c021-16ca-4a76-8a5d-671a50ed013b.fullname" . }}
  labels:
    {{- include "go-echof373c021-16ca-4a76-8a5d-671a50ed013b.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echof373c021-16ca-4a76-8a5d-671a50ed013b.selectorLabels" . | nindent 4 }}
