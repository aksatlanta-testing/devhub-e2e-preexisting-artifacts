apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo5ec754b9-25c0-42cf-92d0-d0b79b60df67.fullname" . }}
  labels:
    {{- include "go-echo5ec754b9-25c0-42cf-92d0-d0b79b60df67.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo5ec754b9-25c0-42cf-92d0-d0b79b60df67.selectorLabels" . | nindent 4 }}
