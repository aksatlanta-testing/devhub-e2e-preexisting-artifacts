apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echoc5c8fffe-30db-492a-a5ad-163b0e079926.fullname" . }}
  labels:
    {{- include "go-echoc5c8fffe-30db-492a-a5ad-163b0e079926.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echoc5c8fffe-30db-492a-a5ad-163b0e079926.selectorLabels" . | nindent 4 }}
