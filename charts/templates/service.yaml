apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echod3f8d2b6-dfce-4b5e-80f2-cd936b81c2c6.fullname" . }}
  labels:
    {{- include "go-echod3f8d2b6-dfce-4b5e-80f2-cd936b81c2c6.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: {{.Values.generatorLabel}}
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echod3f8d2b6-dfce-4b5e-80f2-cd936b81c2c6.selectorLabels" . | nindent 6 }}
