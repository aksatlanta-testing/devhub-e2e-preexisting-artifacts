apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echod6bbb289-abc3-48d5-ada8-85ab0f5e9dee.fullname" . }}
  labels:
    {{- include "go-echod6bbb289-abc3-48d5-ada8-85ab0f5e9dee.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echod6bbb289-abc3-48d5-ada8-85ab0f5e9dee.selectorLabels" . | nindent 4 }}
