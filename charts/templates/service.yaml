apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echob8408345-b1c0-4f60-b2d1-5bad4f2db306.fullname" . }}
  labels:
    {{- include "go-echob8408345-b1c0-4f60-b2d1-5bad4f2db306.labels" . | nindent 4 }}
    kubernetes.azure.com/generator: devhub
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echob8408345-b1c0-4f60-b2d1-5bad4f2db306.selectorLabels" . | nindent 4 }}
