apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo373cf516-30fa-44c1-a5ac-d292d032067c.fullname" . }}
  labels:
    {{- include "go-echo373cf516-30fa-44c1-a5ac-d292d032067c.labels" . | nindent 4 }}
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo373cf516-30fa-44c1-a5ac-d292d032067c.selectorLabels" . | nindent 6 }}
